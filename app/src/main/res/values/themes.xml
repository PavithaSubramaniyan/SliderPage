<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SplitProject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryVariant">@color/green</item>
        <item name="colorOnPrimary">@color/background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/grey</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="BottomNavigation" parent="Widget.MaterialComponents.BottomAppBar">
        <item name="tint">@color/green</item>
        <item name="backgroundTint">@color/background</item>
    </style>
    <style name="TabIndicatorColor" parent="Theme.MaterialComponents">
        <item name="android:textColor">@color/green</item>
        <item name="tabIndicatorColor">@color/green</item>
        <item name="tabBackground">@color/background</item>
    </style>
</resources>